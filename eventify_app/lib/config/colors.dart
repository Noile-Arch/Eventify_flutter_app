import 'package:flutter/material.dart';

// Primary colors
const mainColor = Color(0xFFF6F8FF);  // Softer background
const primaryColor = Color(0xFF4A6CFF);  // Vibrant blue as primary
const primaryLight = Color(0xFF738FFF);  // Lighter blue for highlights

// UI Element colors
const barColor = Color(0xFF2D3142);  // Dark slate for bottom nav
const buttonColor = Color(0xFF4A6CFF);  // Same as primary
const accentColor = Color(0xFFFF6B6B);  // Coral accent for highlights

// Navigation colors
const navSelected = Color(0xFF4A6CFF);    // Selected nav item (primary blue)
const navUnselected = Color(0xFF9BA0AB);  // Unselected nav items (light gray)
const navBackground = Color(0xFFFFFFFF);  // White background with elevation

// Text colors
const text1 = Color(0xFF2D3142);  // Dark slate for primary text
const text2 = Color(0xFF636773);  // Medium gray for secondary
const text3 = Color(0xFF9BA0AB);  // Light gray for tertiary

// Background & Surface colors
const inputFill = Color(0xFFF0F3FF);  // Light blue-tinted input
const cardFill = Color(0xFFFFFFFF);  // White cards
const surfaceColor = Color(0xFFF6F8FF);  // Same as mainColor

// Status colors
const successColor = Color(0xFF4CAF50);  // Green
const errorColor = Color(0xFFFF5252);    // Red
const warningColor = Color(0xFFFFB300);  // Amber
